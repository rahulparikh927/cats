import React from "react";

type Props = {};

const Header = (props: Props) => {
  return (
    <header className="appHeader">
      <h3>Cat Application</h3>
    </header>
  );
};

export default Header;
